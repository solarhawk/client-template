apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# This root kustomization points to base for standalone use.
# For environment-specific deployments, use overlays/dev or overlays/prod.
resources:
  - base
