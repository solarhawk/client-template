apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Online Boutique - App-of-Apps pattern
# This deploys the parent resources; ApplicationSet generates per-service apps
resources:
  - namespace.yaml
  - argocd-project.yaml
  - applicationset.yaml
