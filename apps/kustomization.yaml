apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Client applications to deploy alongside the base chart
# These demonstrate different deployment patterns:
resources:
  # Helm-based app: Google's Online Boutique (microservices-demo)
  # Uses GitRepository + HelmRelease pattern
  - microservices-demo

  # Manifest-based app: Simple whoami service
  # Uses raw Kubernetes manifests (Deployment, Service, etc.)
  - whoami

  # Add your custom apps below:
  # - my-custom-app
