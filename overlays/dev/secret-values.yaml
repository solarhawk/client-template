# Development environment SECRET values
# These values are stored in a Secret for sensitive data
# DO NOT commit real secrets to version control!

# Registry credentials (if using private registry)
registryCredentials:
  registry: ""
  username: ""
  password: ""
  email: ""

# Git credentials (if using private git repos)
git:
  credentials:
    username: ""
    password: ""

# GitLab Runner token (when enabled)
gitlabRunner:
  values:
    runnerToken: ""
